[project]
name = "ex1"
version = "0.1.0"
description = ""
authors = [
    {name = "kanahiz",email = "karimnahiz18@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas (==2.1.0)",
    "numpy (==1.25.0)",
    "requests (==2.31.0)",
    "matplotlib (==3.7.2)"
]

[tool.poetry]
packages = [{include = "ex1", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
